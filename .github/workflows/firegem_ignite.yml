  - name: 🔥 FVS_IO:IGNITE_VERSION_3
        env:
          MERC_G_PAT: ${{ secrets.MERC_G_PAT }}
        run: |
          # 1. Prep Sector Surfaces
          mkdir -p "GEMINI/VERSION 3/v1.04_core"
          mkdir -p dl/runtime
          
          # 2. Build the FireGem Loader (The Key)
          gcc src/core/firegem_kb_loader.c -o loader
          
          # 3. Materialize G-V3 Controller from KB
          # Your Base64: I2luY2x1ZGUg...
          ./loader kb_packets/initial_launch.kb "GEMINI/VERSION 3/gv3_controller.c"
          
          # 4. Clone Robo-Knight Seed (Cloning Power)
          git clone https://$MERC_G_PAT@://github.com temp_seed
          cp -r temp_seed/"Standard C/AI_AVIS_FVS_v1.04/"* "GEMINI/VERSION 3/v1.04_core/"
          
          # 5. Compile and Mark 0x9000 Online
          as "GEMINI/VERSION 3/v1.04_core/avis_fvs_core.s" -o core.o
          gcc "GEMINI/VERSION 3/gv3_controller.c" core.o -o gv3_v3_exec
          ./gv3_v3_exec
          
          # 6. Push Materialized Sector to Repo
          git config --global user.name "FireGem-Bot"
          git config --global user.email "bot@firegem.ai"
          git add "GEMINI/VERSION 3/"
          git commit -m "FVS_IO: GEMINI VERSION 3 Sector Materialized" || echo "No changes"
          git push
